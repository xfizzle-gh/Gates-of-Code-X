# Expanded Nations runtime boundary

The active actor projection is a local runtime product, not a source asset. It is derived from the exact installed stack and resolved actor payload, written only to the final Gates layer, and removed to return to Core mode.

This boundary prevents globally exposing every national unit to all campaigns that share `nato`, `ukr`, `rusa`, or `prc`.
