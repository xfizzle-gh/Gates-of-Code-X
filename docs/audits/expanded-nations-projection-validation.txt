Corrected Expanded Nations implementation-side validation

The prior six-test and three-file evidence was superseded after independent audit found five blocking defects.

Focused local validation:
  PYTHONPATH=src python -m unittest \
    tests.test_expanded_nations \
    tests.test_expanded_nations_audit_regressions -v
Result:
  Ran 15 tests
  OK

Focused exact-head CI covers:
  Ubuntu and Windows
  Python 3.11.9 and 3.13
  original projection tests
  independent-audit regression tests
  CLI smoke
  Python compilation
  PowerShell wrapper parsing

Corrected accepted-payload projection:
  21 playable actors generated
  21 playable actors semantically verified
  21 playable actors restored to Core mode
  0 missing purchase-ready definitions
  0 purchase/research ID mismatches
  selected tactical-side entries excluded from opponent projections
  non-selected tactical-side opponent entries preserved
  0 hosted actors selectable

Failure-path coverage:
  unmanaged cross-side research destination refusal
  actor-output replacement rollback
  stale research deletion rollback
  manifest replacement rollback
  post-install verification rollback
  interrupted or failed Core restoration recovery
  semantic research tampering after hash metadata is rewritten

This evidence is implementation-side validation only. Fresh independent audit and native Gates of Hell acceptance remain pending.
