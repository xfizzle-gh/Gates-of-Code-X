Expanded Nations implementation-side validation status

The previously complete 21-playable-actor schema-3 projection matrix is
invalidated after native Serbia acceptance at exact head
0926d347138738f75205237b75fef1145383d77f.

Native evidence proved that implementation-side syntax and hash verification did
not cover four engine contracts:

  * RUSA replacement must exclude the inherited SOV/CSA purchase family.
  * Soviet-pattern equipment exported to RUSA must use period(2022s) and native
    rus_vehicleman/rus_supporter crew aliases.
  * Actor-created Serbia squad IDs require native squad localization and four
    portrait variants.
  * Native research must emit purchase IDs rather than opaque generated tech IDs.

The correction changes generated actor, opponent, research, presentation, and
manifest bytes. All prior actor counts and signatures are stale.

Required next evidence:

  run the focused and full exact-head workflows
  obtain fresh independent implementation review
  run the exact-stack matrix from clean Core mode
  commit the complete 21-actor JSON and Markdown outputs
  obtain fresh evidence review
  resume Serbia only after those gates pass

Do not proceed to DPRK or merge PR #172.
